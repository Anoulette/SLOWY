<%= simple_form_for [@vehicle, @booking], data: { controller: "flatpickr", flatpickr_dates_value: @dates_booked } do |f| %>
  <%= f.input :date, as: :string, input_html: { data: { flatpickr_target: "date" } } %>
  <%= f.button :submit,"Booking", class:"button primary"%>
<% end %>
